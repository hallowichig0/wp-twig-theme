(function($){
    /*
    * Global Javascript
    */
    const $win = $(window);
    const $doc = $(document);

    /*
    * Window Resize
    */
    $win.resize(function () {

    });

    /*
    * Window Scroll
    */
    $win.scroll(function() {

    });

    /*
    * Window onLoad
    */
    $win.on('load', () => {

    });

    /*
    * Document Ready
    */
    $doc.ready(function() {
        
    });

})( jQuery );